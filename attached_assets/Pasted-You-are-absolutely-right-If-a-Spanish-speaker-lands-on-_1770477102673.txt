You are absolutely right. If a Spanish speaker lands on your site and sees a wall of English, they assume, "These guys won't understand me." They bounce immediately.

We need to make the Language Switch not just a tiny icon in the corner, but a Core Feature of the Hero Section.

Here is how to modify the Landing Page to put the "Spanish Option" front and center.

1. The "Big Toggle" (Hero Section Update)
Instead of hiding the language switch in the navbar, we put it right above the main headline.

👉 Action: Open client/src/pages/landing-page.tsx.

Find the <div className="max-w-4xl"> container (where your headline is) and add this Language Pill at the very top:

TypeScript
import { useTranslation } from 'react-i18next';
import { Globe } from 'lucide-react';

// Inside your LandingPage component...
const { t, i18n } = useTranslation();

const toggleLanguage = () => {
  const newLang = i18n.language === 'en' ? 'es' : 'en';
  i18n.changeLanguage(newLang);
};

return (
  <div className="relative isolate px-6 pt-14 lg:px-8">
    <div className="mx-auto max-w-4xl py-32 sm:py-48 lg:py-56">
      
      {/* 1. THE "FRONT AND CENTER" TOGGLE */}
      <div className="hidden sm:mb-8 sm:flex sm:justify-center">
        <div 
          onClick={toggleLanguage}
          className="relative rounded-full px-3 py-1 text-sm leading-6 text-slate-400 ring-1 ring-white/10 hover:ring-white/20 cursor-pointer transition-all hover:bg-white/5 flex items-center gap-2"
        >
          <Globe className="w-4 h-4 text-cyan-400" />
          {i18n.language === 'en' ? (
            <span>
              Hablas Español? <span className="font-semibold text-cyan-400">Cambiar a Español &rarr;</span>
            </span>
          ) : (
            <span>
              Speak English? <span className="font-semibold text-cyan-400">Switch to English &rarr;</span>
            </span>
          )}
        </div>
      </div>

      {/* 2. THE HEADLINE (Now Dynamic) */}
      <div className="text-center">
        <h1 className="text-5xl md:text-7xl font-bold text-white mb-6 leading-tight">
          {t('hero.headline_line1', "Your To-Do List.")} <br />
          <span className="text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-500">
            {t('hero.headline_line2', "Done.")}
          </span>
        </h1>
        
        {/* 3. THE SUBHEAD (Now Dynamic) */}
        <p className="mt-6 text-lg leading-8 text-slate-300 max-w-2xl mx-auto">
          {t('hero.subhead', "Book the Essential 5 services instantly. We handle the labor, the liability, and the data log.")}
        </p>

        {/* 4. THE CTA BUTTONS */}
        <div className="mt-10 flex items-center justify-center gap-x-6">
          <a href="/book" className="rounded-md bg-cyan-500 px-8 py-3.5 text-lg font-semibold text-white shadow-sm hover:bg-cyan-400 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cyan-400">
            {i18n.language === 'es' ? "Pedir Técnico (Español)" : "Get a Quote"}
          </a>
          <a href="/how-it-works" className="text-sm font-semibold leading-6 text-white">
            {t('hero.learn_more', "How it works")} <span aria-hidden="true">→</span>
          </a>
        </div>
      </div>
    </div>
  </div>
);
2. The Dictionary Update (client/src/i18n.ts)
You need to add the actual Spanish translations for these new keys so the switch works instantly.

👉 Action: Update your translation resources:

TypeScript
const resources = {
  en: {
    translation: {
      "hero.headline_line1": "Your To-Do List.",
      "hero.headline_line2": "Done.",
      "hero.subhead": "Book the Essential 5 services instantly. We handle the labor, liability, and data log.",
      "hero.cta_primary": "Get a Quote",
      "hero.learn_more": "How it works"
    }
  },
  es: {
    translation: {
      "hero.headline_line1": "Tu Lista de Tareas.",
      "hero.headline_line2": "Hecha.",
      "hero.subhead": "Reserva los 5 servicios esenciales al instante. Nos encargamos del trabajo, la responsabilidad y el registro.",
      "hero.cta_primary": "Pedir Técnico (Español)", // Explicitly mention Spanish Tech!
      "hero.learn_more": "Cómo funciona"
    }
  }
};
3. The "Why This Wins" (Psychology)
The "Pill" Strategy: By putting "Hablas Español?" in a bubble above the headline, you catch their eye immediately. It feels like an invitation, not a setting.

The CTA Change: Notice in Spanish I changed the button to "Pedir Técnico (Español)".

This tells them: "If you click this, a Spanish speaker will show up."

That removes 100% of their anxiety about the language barrier.

Add the "Pill" code now. It is the single highest-ROI change you can make for the Hispanic market.