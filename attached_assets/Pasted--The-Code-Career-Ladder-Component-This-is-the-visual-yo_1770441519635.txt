💻 The Code: "Career Ladder" Component
This is the visual you need for the Pycker page. It shows them they aren't stuck at the bottom; they can "Level Up."

File: client/src/components/marketing/career-ladder.tsx

TypeScript
import { Trophy, Truck, ShieldCheck, TrendingUp, Users } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";

export function CareerLadder() {
  const levels = [
    {
      level: 1,
      title: "The Rookie",
      color: "bg-slate-100 border-slate-300",
      icon: <Truck className="w-8 h-8 text-slate-600" />,
      perks: [
        "Instant Daily Payouts",
        "Access to Junk & Moving Jobs",
        "We Cover Your Insurance",
        "Keep 100% of Tips"
      ],
      income: "$500 - $800 / wk"
    },
    {
      level: 2,
      title: "Verified Pro",
      color: "bg-blue-50 border-blue-200",
      icon: <ShieldCheck className="w-8 h-8 text-blue-600" />,
      perks: [
        "Unlocks Pressure Washing & Gutters",
        "LOWER Fees (Keep 80%)",
        "Priority Dispatch (See jobs first)",
        "Verified Badge on Profile"
      ],
      income: "$1,000 - $1,500 / wk"
    },
    {
      level: 3,
      title: "Master Consultant",
      color: "bg-amber-50 border-amber-200",
      icon: <Trophy className="w-8 h-8 text-amber-600" />,
      perks: [
        "Paid for $49 Home Audits",
        "10% Commission on Upsells",
        "Dedicated Account Manager",
        "Zero-Fee 'Pass' Once a Month"
      ],
      income: "$2,000+ / wk"
    }
  ];

  return (
    <div className="py-12 bg-white">
      <div className="text-center mb-10">
        <h2 className="text-3xl font-bold text-slate-900"> Climb the Ladder </h2>
        <p className="text-slate-600 mt-2">We don't just give you jobs. We build your career.</p>
      </div>

      <div className="grid md:grid-cols-3 gap-6 max-w-6xl mx-auto px-4">
        {levels.map((lvl) => (
          <Card key={lvl.level} className={`relative border-2 ${lvl.color} hover:shadow-lg transition-shadow`}>
            {/* Badge */}
            <div className="absolute -top-5 left-1/2 -translate-x-1/2 bg-white p-3 rounded-full shadow-sm border">
              {lvl.icon}
            </div>

            <CardContent className="pt-12 pb-6 text-center">
              <h3 className="text-xl font-bold mb-1">{lvl.title}</h3>
              <p className="text-sm font-semibold text-green-700 mb-6">{lvl.income}</p>

              <ul className="space-y-3 text-left text-sm px-4">
                {lvl.perks.map((perk, i) => (
                  <li key={i} className="flex items-start">
                    <TrendingUp className="w-4 h-4 mr-2 text-green-600 shrink-0 mt-0.5" />
                    <span className="text-slate-700">{perk}</span>
                  </li>
                ))}
              </ul>
            </CardContent>
          </Card>
        ))}
      </div>
      
      <div className="mt-10 text-center">
         <button className="bg-primary text-primary-foreground px-8 py-4 rounded-full text-lg font-bold hover:bg-primary/90 transition shadow-xl">
            Start as a Rookie Today
         </button>
      </div>
    </div>
  );
}
🚀 How to use this
Copy the Code: Create the file client/src/components/marketing/career-ladder.tsx and paste the code above.

Import it: On your Pycker Landing Page (client/src/pages/hauler-landing.tsx), add <CareerLadder />.

Update the Text: Go to your Customer Landing Page and paste the "Hero Section" text I wrote above.